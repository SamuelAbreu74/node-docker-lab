# Mini API Node.js com Docker

Projeto para praticar conceitos básicos de conteinerização.

## Como rodar o projeto

1. Build da imagem: `docker build -t mini-node-api .`
2. Execução do container: `docker run -d -p 3000:3000 mini-node-api`
3. Acesse `http://localhost:3000`